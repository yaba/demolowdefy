import { Options } from "../../../core";
import { AnyVal, RawObject } from "../../../util";
/**
 * Converts a date/time string to a date object.
 * @param obj
 * @param expr
 */
export declare function $dateFromString(obj: RawObject, expr: AnyVal, options?: Options): AnyVal;
